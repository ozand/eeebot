# Lesson index

| lesson | prevents | tags |
|---|---|---|
| [Adjacent and Dependent Test Suite Pre-Commit Verification Protocol](adjacent_test_suite_verification.md) | Before committing, identify every test suite that is adjacent to or dependent on the changed files and run all of them — not just the target suite. | test |
| [Immediate Skip Emission When the Proposed Change Already Exists](already_implemented_skip_emission.md) | Evaluate skip eligibility at the start of inspection, not the end: read the designated target file once, check the "Recent activity" list, and if that primary inspection proves the change is already present, immediately emit the structured |  |
| [Avoid Bundled Test Executions](avoid_bundled_test_executions.md) | **Default to targeted, single-suite verification.** The in-cycle rule is "one test file per `exec` call"; the gate owns full-suite runs. | test |
| [Do Not Inspect Historical State Reports When Verifying Script Capabilities](avoid_state_report_inspection.md) | **Match the evidence to the question.** Use the decision table below before reaching for any file. | state |
| [Avoiding Repeat Failures](avoiding_repeat_failures.md) | We prevent repeat failures by following correct archival procedures, verifying changes before committing, and targeting mutation surfaces. | git |
| [Avoiding Repeated Task Failures by Checking Recent Cycle Outcomes](avoiding_repeated_failures.md) | **Verify Recent Outcomes**: Always check if the task or a closely related task has been attempted in the last 7 days by inspecting `memory/HISTORY.md`. |  |
| [Batching Consecutive File Edits to Minimize Round Trips](batch-consecutive-file-edits.md) | **Default to one tool call per file per logical change.** Before issuing an `edit_file`, ask: "are there other hunks in this same file I already know I need?" If yes, fold them into this call (wider span or full-file `write_file`) instead |  |
| [Blocked File Present Failure Mode and Remediation](blocked_file_remediation.md) | **Strict Naming Conventions**: Avoid using words like "secret", "token", "credential", or "private_key" in filenames (even for test files or documentation, unless absolutely necessary and allowed). |  |
| [Bounded Initial Reconnaissance](bounded_initial_reconnaissance.md) | Enforce the turn-1-4 sequence: read the designated target once, confirm it is not already done (Recent activity list plus at most one targeted grep/git log), read at most one companion file, and begin editing by turn Treat "still reading |  |
| [Chained git status / add / commit / log workflow](chained_git_commit_workflow.md) | For any cycle whose only remaining work is committing a known target file, issue the status/add/commit/log sequence as one `exec` call joined with `&&` instead of four separate calls. | git |
| [Verify Existing Test Files and Commits Before Queuing Unit Test Proposals](check_existing_tests_before_proposals.md) | Treat "does the target artifact already exist?" as a mandatory first step for any proposal whose deliverable is a file (tests, scripts, lessons, docs). | test |
| [Verify Docstring Exit Code Tables Before Asserting CLI Subprocess Return Codes](cli_subprocess_exit_codes.md) | Treat the docstring exit code table as the contract: never assert a `returncode` without first locating the row that covers the scenario. |  |
| [Isolate CLI Verification State to Prevent Repo File Mutation](cli_verification_state_isolation.md) | **Treat every script execution as potentially mutating.** Assume a run writes something until you have confirmed otherwise (dry-run flag, `--help` output, or a post-run `git status` check). | state |
| [Late-Turn Concise Tool Execution and Test Output Truncation](concise_late_turn_tool_outputs.md) | In the late turns of a cycle, keep every tool call's output bounded: use `read_file` with `offset`/`limit`, cap test runs with `-q` and `tail`, and summarize large results already in context instead of re-running the command. | test |
| [Coupling AGENTS.md Standing Instruction Updates with Structural Test Assertions](coupling_agents_instructions_with_structural_tests.md) | **Rule of thumb:** if you add a `## ` section to `AGENTS.md` that states a standing instruction (a protocol, a ban, a required behavior), the same commit must add its entry to `REQUIRED_SECTIONS` in `tests/test_agents_structure.py`, plus a | test |
| [Dual Mock and Live Ledger Verification Protocol](dual_ledger_testing_pattern.md) | Pair every ledger-reading script with both verification layers: a mock layer in `tmp_path` covering missing/empty/malformed/boundary cases, and a live layer doing one bounded read-only run against the real ledger with output capped and |  |
| [Dual-Sink Output Pattern for Sampler Migrations](dual_sink_sampler_pattern.md) | Apply this protocol to every sampler migration so the downstream graph stays **Inventory the sinks before touching the producer.** Grep the repo for every path the old sampler wrote to, and for every script that reads those paths (`grep |  |
| [Early-Turn Edit Budget Discipline and Inspection Limits](early_edit_turn_discipline.md) | **Treat the target file as the only file that matters.** The task names a target path. |  |
| [Early-Turn Implementation Protocol](early_turn_implementation_protocol.md) | **Treat the turn-3 first write as a hard rule, not a suggestion.** The budget-allocation ceiling ("first edit by iteration 20") is the outer bound; the practical start is turn 3. |  |
| [Enforcing AGENTS.md Sections via REQUIRED_SECTIONS in tests/test_agents_structure.py](enforcing_agents_sections_in_structural_tests.md) | **Rule of thumb:** if you add a `## ` section to `AGENTS.md` that states a standing instruction (a protocol, a ban, a required behavior), the same commit must add its entry to `REQUIRED_SECTIONS` in `tests/test_agents_structure.py`, plus a | test |
| [Existence Index Duplicate Skip Mode and Mitigation](existence_index_duplicate.md) | **Check Existing Scripts**: Before proposing or implementing a task, search the repository (e.g., in `scripts/`, `nanobot/`, etc.) to ensure a similar script does not already exist. |  |
| [Resolving checkout_main_failed and Git Lock Issues on Constrained Hosts](git_checkout_recovery.md) | **Automated Lock Cleanup**: Implement pre-flight checks in coordinator and subagent scripts to detect and automatically remove `index.lock` if it is older than a threshold (e.g., 5 minutes) and no git process is running. | git |
| [Handling Git Push Rejections (`push_rejected`)](handling_push_rejections.md) | **Always Pull Before Working**: The coordinator and subagents must run `git pull` (or `git fetch` + `git rebase`) at the start of every cycle. | git |
| [Incremental Staging for Script Modifications](incremental_script_modifications.md) | **Default to incremental staging for any multi-part script change.** If a task names more than one behavior to add or change in a script, plan it as N discrete edits, each with its own verify step and its own commit. |  |
| [Inspect Standard Library Class Attributes and Return Types Before Code Edits](inspect-stdlib-attributes.md) | **Probe before you glue.** Any time you write code that calls a standard library method you have not called earlier in the same session, run a one-liner (`python3 -c "..."`) that (a) lists the class's public attributes via `dir()`, (b) |  |
| [Inspect CLI Parser Signatures Before Script Invocation](inspect_cli_signatures_before_invocation.md) | **Probe the parser signature, not just the help text, for any script not invoked earlier in the same session.** `grep -n "add_argument" <script>` reveals the positional/named split that a `--help` usage line only hints at; use `--help` |  |
| [Inspect Script CLI Flags Before Invoking Validation Scripts](inspect_script_cli_flags.md) | **Default to a one-shot interface probe for any script not invoked earlier in the same session.** `--help` is the canonical probe; a bounded `grep -n "add_argument" <script>` is the fallback when the script has no help text or the output |  |
| [Checkout Main Failed Failure Mode and Remediation](lesson_checkout_main_failed.md) | **Workspace Hygiene**: Ensure the coordinator and subagents clean up any temporary files they create. |  |
| [Paired Markdown Format and Code Block Validation Protocol for Documentation Edits](markdown_code_block_validation_protocol.md) | Make the paired run a fixed pre-commit step for any markdown edit: format validator, code block validator, then the two paired pytest suites, in that order, before `git add`. |  |
| [Defining Contracts for Mixed Valid and Invalid Inputs in Diagnostic Recommendations](mixed_input_diagnostic_contracts.md) | **Write the collection contract before the code.** For any script whose input is a list/collection of items, add a docstring decision table covering all-valid, mixed, and all-invalid cases *before* implementing, so the test and the |  |
| [Preventing Mutation-Surface Violation Retries](mutation_surface_violation_retries.md) | Surface validation is a precondition, not a post-commit cleanup step. |  |
| [Use nargs='?' for positional CLI args alongside standalone flags](optional_positional_args_with_standalone_flags.md) | When adding a standalone flag (`--test`, `--version`, `--dry-run`, etc.) to a script that also takes a positional argument, always define the positional with `nargs="?"` and a `default`, and validate its presence in code after the |  |
| [out_of_band_main_detected — Root Cause and Remediation](out_of_band_main_detected.md) | **Keep the bridge loop serial and the detector fail-open.** The current design (positive-only, fail-open, lease-protected push) is correct; do not "fix" the false positive by making the detector stricter or by auto- integrating despite | gate |
| [Persist Refactored Code to Disk and Verify Before Ending the Cycle](persist_refactored_code_before_cycle_end.md) | Treat "benchmark succeeded" as a *trigger to edit*, never as a terminal state: the same tool call that reports the timing should be followed by the file write in the next step. |  |
| [Pre-Creation Tool Chain Limits and Fast-Path Writing Protocol](pre_creation_tool_chain_limits.md) | **New-file tasks are context-risk tasks.** The absence of a target file to read is a signal to *write sooner*, not to explore more. |  |
| [Preventing Duplicate Proposals and Blocked File Failures](prevent_duplicate_proposals.md) | Never use blocked words (`secret`, `token`, `credential`, `private_key`, etc.) in filenames, even for tests or documentation. |  |
| [Repeat Failures Prevention Guide](prevent_repeat_failures.md) | We prevent repeat failures by: Running pre-flight checks on the workspace/git diff. |  |
| [Preventing Git Checkout Failures during Autonomous Cycles](preventing_git_checkout_failures.md) | To prevent git checkout failures from occurring during autonomous cycles, the following strategies must be implemented: Ensure all scripts and tests clean up their temporary files. | git |
| [Preventing Turn-Limit Exhaustion: Direct Read-Edit-Verify-Commit Workflow](preventing_turn_limit_exhaustion.md) | **Treat the target as the only file that matters.** The task names a target path; your job is to edit that path. |  |
| [Improving Proposal Confirmation Ratio and Avoiding Common Rejection Patterns](proposal_confirmation_guidelines.md) | To maintain a confirmation ratio above 0.5 and avoid common rejection patterns, the agent must adhere to the following guidelines: **Ensure Functional Value**: Every proposal must deliver concrete, functional value. |  |
| [Proposal Confirmation Standards to Improve Confirmation Ratio](proposal_confirmation_standards.md) | To prevent low confirmation ratios and ensure all future proposals are confirmable, the following standards must be enforced: **Deduplication Checks**: Before proposing a new task, search the repository for existing scripts or similar past |  |
| [Avoiding `rm -rf` in shell commands in favor of `tempfile` for temporary test artifacts](safe_temp_artifacts.md) | **Default to `tempfile` / `tmp_path`** for every temporary artifact in scripts and tests. | test |
| [Refined Secret-Scan Patterns: Avoiding False Alarms on Documentation](secret_scan_verification_patterns.md) | Verify every "secret found" hit through the three layers before acting: (1) classify path vs content match — path matches in non-runtime trees are exempt; (2) for content hits, check whether the line assigns a value to a sensitive name or | test |
| [Shared Parsing Helper Pattern for Consistent Metric Derivation](shared_parsing_helpers.md) | Centralize parsing in one shared helper and pin its contract with tests: missing file → `()`, malformed line → skipped, non-outcome phase → excluded, ordering → newest-first. |  |
| [Structuring Skills in Subdirectories as skills/<skill_name>/SKILL.md](skill-subdirectory-structure.md) | Before writing or editing any skill file, resolve the target to the canonical form `skills/<name>/SKILL.md` and confirm the directory exists (create it if needed). |  |
| [Modular Skill Composition for Multi-Phase Workflow Orchestration](skill_composition_orchestration.md) | Plan the phase list (name, skill referenced, input, output) before the first edit; if a phase cannot be stated in one line, split it. |  |
| [Operational Pitfall Checks for Skill Validation](skill_validation_pitfalls.md) | Make the two pitfall checks a fixed part of any skill that reports on agent activity: a same-timezone comparison rule and a "blocked stub is not inactivity" rule, each with an explicit pass condition. |  |
| [Speculative Read Avoidance and Existence Verification Protocol](speculative_read_prevention.md) | **A path in a prompt is a claim, not a fact.** Confirm it once, cheaply, before reading. |  |
| [splitlines() trailing newline pitfall in line normalizers](splitlines_trailing_newline.md) | When writing or reviewing any line-oriented helper, decide the trailing newline contract up front and state it in the docstring (e.g. |  |
| [Subagent Result Error Triage and Recovery](subagent_result_error_handling.md) | **Triage from the result file, not by re-running.** On any `status: "error"` subagent result, the first action is to read `state/subagents/archive/<id>.json` and classify using the table above. | subagent |
| [Regex Boundaries and Distinct Synthetic Strings for Substring Test Assertions](substring_matching_boundaries_in_tests.md) | **Never assert a numbered item with a raw substring check** when the fixture or output may contain a longer sibling with the same prefix. | test |
| [Target-Scoped Repo Inspection Protocol](target_scoped_inspection_protocol.md) | **Scope is defined by the task, not by curiosity.** The task names the target; everything else is out of scope until a direct dependency is proven. |  |
| [Capturing Edge-Case Test Anomalies and Remediation Handoffs](test_scope_anomaly_handoffs.md) | **Pin, don't hide.** When a tests-only cycle discovers a script anomaly, write a test that asserts the *current* behavior and annotate it with the marker `# KNOWN DEFECT (handoff):` followed by: the exact invocation that triggers it, the | test |
| [Uncooled Script Reflection Handling Policy](uncooled_reflection_handling.md) | **Treat the named target as a hint, not a mandate, for reflection demands.** The demand's intent (the learning to be extracted) outranks the named path. |  |
