# Design: <short title>

_Optional — include only when the "how" is non-obvious or has trade-offs worth
recording. For a simple change, `proposal.md` alone is enough._

## Approach

How the change is implemented at the right altitude (generalize the underlying
mechanism rather than layering special cases).

## Affected components

- `path/to/module.py` — what changes
- ...

## Trade-offs / alternatives considered

Why this approach over the obvious alternatives. What we are accepting.

## Verification

How we prove it works (tests added, commands run, observable artifacts).

## Spec delta

Link to `specs/<capability>/spec.md` in this change folder if requirements change,
or describe the requirement(s) added/removed/modified.
