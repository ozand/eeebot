# Fleet operations

Moved into the knowledge base so agents can find it by search rather than by
knowing the path: **`kb/wiki/fleet-operations.md`**.

Look it up with `kb-bootstrap search`, or read the file directly.
